<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android">
    <group android:checkableBehavior="single">
        <!-- 1. Add a "Home" item to go back to the main menu -->
        <item
            android:id="@+id/nav_home"
            android:icon="@android:drawable/ic_menu_myplaces"
            android:title="Home" />

        <item
            android:id="@+id/nav_appointments"
            android:icon="@android:drawable/ic_menu_my_calendar"
            android:title="Appointments" />

        <item
            android:id="@+id/nav_patient_summary"
            android:icon="@android:drawable/ic_menu_agenda"
            android:title="Patient Summary (EHR)" />

        <item
            android:id="@+id/nav_register_patient"
            android:icon="@android:drawable/ic_menu_add"
            android:title="Register Patient" />

        <item
            android:id="@+id/nav_admin_portal"
            android:icon="@android:drawable/ic_menu_manage"
            android:title="Admin Portal" />
    </group>

    <item android:title="Account">
        <menu>
            <item
                android:id="@+id/nav_logout"
                android:icon="@android:drawable/ic_menu_revert"
                android:title="Logout" />
        </menu>
    </item>
</menu>
